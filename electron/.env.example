# ── Electron dev overrides ────────────────────────────────────────────────────
# Set to 'development' to open DevTools automatically
NODE_ENV=production

# Set to '1' to load the live remote server instead of local frontend/dist
# Useful if you want to test against production without building frontend
ELECTRON_REMOTE=0
